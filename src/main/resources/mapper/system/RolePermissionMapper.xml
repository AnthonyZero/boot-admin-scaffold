<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anthonyzero.scaffold.system.mapper.RolePermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.anthonyzero.scaffold.system.entity.RolePermission">
        <id column="role_permission_id" property="rolePermissionId" />
        <result column="role_id" property="roleId" />
        <result column="permission_id" property="permissionId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        role_permission_id, role_id, permission_id
    </sql>

    <!-- 递归删除用户权限，findPermsChildren为自定义的 MySQL函数，作用为根据当前 权限ID递归查找出其所有下级权限ID包括自己-->
    <select id="deleteRolePermissions" parameterType="string">
        DELETE from role_permission
        WHERE permission_id in
        (
            SELECT t.permission_id FROM
            (
              SELECT permission_id FROM permission WHERE FIND_IN_SET( permission_id, findPermsChildren ( #{permissionId} ) ) > 0
            ) t
        )
    </select>
</mapper>
